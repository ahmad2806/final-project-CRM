export class User {
    constructor(public name: string, public phone: string, public email: string, public username: string,
        public password: string, public VolPer: boolean, public DonorPer: boolean, public Freeze: boolean) {

    }
}